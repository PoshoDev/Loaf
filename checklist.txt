[ ] sfw() with passable lists.
[ ] query() with passable lists or a multi() function. The idea is to execute
    all queries and if one fails, the whole thing does not commit.
[ ] Support for more databases (Firebase? NoSQL?).
[ ] Support for local databases with SQLite.
[ ] Loaf objects, so you can use more than one session.
[ ] More cursor types.
[ ] Log options.
[ ] Encryption/decryption tools.
[ ] Tables, functions, etc.; as variables, so you can do:
    session.users.all()
    session.users.get("id=1")
    session.function(arg)
